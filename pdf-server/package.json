{
  "name": "bankmaps-pdf-server",
  "version": "1.0.0",
  "description": "Headless Playwright PDF renderer for BankMaps",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "install-browsers": "npx playwright install chromium --with-deps"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.44.0"
  },
  "engines": {
    "node": ">=18"
  }
}
